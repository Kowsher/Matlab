a = input('Enter number a:');
b = input('Enter number b:');
c = input('Enter number c:');

if ((a+b>c) && (b+c>a) && (c+a>b))
    disp('regarded as side of tringle')
else
    disp('not regarded as side of tringle')
end
