


A = [1,5,10, 3, 2, 55, 7];
fprintf("The acesding order is: " )
disp(sort(A))
fprintf("The descending order is: ")
disp(sort(A, 'descend'))
mx = max(A), mn = min(A)
fprintf("The maximum  & minimum value are: ", mx, mn)

